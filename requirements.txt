pandas==1.5.3  # For data manipulation (last compatible version for 3.9 as of early 2023)
numpy==1.23.5  # For numerical operations (compatible with 3.9)
matplotlib==3.6.3  # For plotting (bar charts, scatter plots, etc.)
seaborn==0.12.2  # For enhanced visualizations (heatmaps, etc.)
windrose==1.9.0  # For wind rose plots (used in your wind analysis)
scipy==1.9.3  # For statistical analysis (Z-scores, correlations)
scikit-learn==1.2.2  # For outlier detection and additional stats
statsmodels==0.13.5  # For time series analysis (monthly patterns)
plotly==5.14.1  # For interactive plots (optional, if you want hover labels)
jupyter==1.0.0  # For running notebooks (optional, if you're using Jupyter)
pytest==7.2.2  # For testing
flake8==6.0.0  # For linting
black==22.12.0  # For code formatting
isort==5.12.0  # For import sorting
Sphinx==5.3.0  # For documentation (optional)
openpyxl==3.1.2  # For reading/writing Excel files (if your data is in Excel)
pyyaml==6.0  # For YAML file handling (e.g., CI config or metadata)